import 'package:flutter/material.dart';

class KudiTheme {

  ///
  /// colors used in the app
  /// Reperesent all the different colors used in the app. 

  /// 
  /// 
  
  static Color main = Color.fromRGBO(20, 131, 102, 1);
  static Color second = Color.fromRGBO(113, 193, 126, 1);
  static Color background = Color.fromRGBO(242, 244, 248, 1);
  static Color cardBackground = Color.fromRGBO(255, 255, 255, 1);
  static Color labelColor = Color.fromRGBO(121, 196, 134, 1);

  Color get mainC => main;
  Color get secondC => second;
  Color get backgroundC => background;
  Color get cardBackgroundC => cardBackground;
  Color get labelColorC => labelColor;

  ///
  /// app background gradient color
  /// 


  /// 

}
